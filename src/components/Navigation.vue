<template>
  <div id="nav">
    <router-link to="/" id="logo">Home</router-link>
    <router-link
      v-for="destination in destinations"
      :key="destination.id"
      :to="{
        name: 'destination.show',
        params: { id: destination.id, slug: destination.slug },
      }"
    >
      {{ destination.name }}
    </router-link>
  </div>
</template>
<script setup>
import SorceData from "@/data.json";
const destinations = SorceData.destinations;
</script>
